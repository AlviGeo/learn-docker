# Melihat network docker
docker network ls

# Membuat network
docker network create --driver namadriver namanetwork

# Menghapus network (tidak dalam digunakan container)
docker network rm namanetwork